'use client'

import React, { memo, useMemo } from 'react';

const FormContact: React.FC = memo(() => {
    return (
        <>
            ôi theo một trong những địa chỉ liên hệ như bên dưới, các chuyên viên của Vinasoy sẽ trực tiếp phản hồi 
        </>
    );
});

export default FormContact;
